# Lock after ten idle minutes. No automatic VM suspend or display power changes.
general {
    lock_cmd = ~/.config/arian-hypr/scripts/actions.sh lock
    before_sleep_cmd = loginctl lock-session
}
listener {
    timeout = 600
    on-timeout = loginctl lock-session
}
